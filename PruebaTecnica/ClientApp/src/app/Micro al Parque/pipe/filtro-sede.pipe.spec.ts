import { FiltroSedePipe } from './filtro-sede.pipe';

describe('FiltroSedePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSedePipe();
    expect(pipe).toBeTruthy();
  });
});
